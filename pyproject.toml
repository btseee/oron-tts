[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "oron-tts"
version = "0.1.0"
description = "F5-TTS (DiT + Flow Matching) for Mongolian Khalkha Dialect"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.11"
authors = [{ name = "OronTTS Team" }]
keywords = ["tts", "mongolian", "flow-matching", "diffusion-transformer"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3.11",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "torch>=2.4.0",
    "torchaudio>=2.4.0",
    "accelerate>=0.33.0",
    "transformers>=4.44.0",
    "datasets>=2.20.0",
    "huggingface-hub>=0.24.0",
    "einops>=0.8.0",
    "torchdiffeq>=0.2.4",
    "librosa>=0.10.2",
    "soundfile>=0.12.1",
    "deepfilternet>=0.5.6",
    "phonemizer>=3.3.0",
    "pyyaml>=6.0.1",
    "rich>=13.7.0",
    "python-dotenv",
    "torchcodec",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
    "ruff>=0.5.0",
    "mypy>=1.10.0",
]

[project.scripts]
oron-train = "scripts.train:main"
oron-infer = "scripts.infer:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*", "scripts*", "configs*"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_configs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-ra -q"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["src"]
section-order = ["future", "standard-library", "third-party", "first-party", "local-folder"]
